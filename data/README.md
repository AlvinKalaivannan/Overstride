# data/

- `raw/` — downloaded datasets, untouched (gitignored)
- `interim/` — intermediate aggregates (gitignored)
- `processed/` — derived feature tables / artifacts (gitignored)

Nothing under here is committed. Each section documents how to (re)populate its inputs.
